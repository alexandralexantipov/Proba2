"""Вариант 1 с выводом результата в терминале"""

# from tkinter import *  # Импортируем все функции из библиотеки tkinter
# root = Tk()  # Создаём главное окно программы
# root.title("Калькулятор")  # Устанавливаем заголовок окна - текст "Калькулятор" будет показан в верхней строке окна интерфейса программы
#
# screen_width = root.winfo_screenwidth()  # Узнаём ширину экрана в пикселях и сохраняем в переменную screen_width
# screen_height = root.winfo_screenheight()  # Узнаём высоту экрана пользователя в пикселях и сохраняем в переменную screen_height
# window_width = 400  # Задаём ширину окна программы в пикселях - 400 точек по горизонтали
# window_height = 230  # Задаём высоту окна программы в пикселях - 230 точек по вертикали
# x = (screen_width - window_width) // 2  # Вычисляем координату X для центрирования окна: от ширины экрана отнимаем ширину окна и делим пополам, чтобы получить отступ слева
# y = (screen_height - window_height) // 2  # Вычисляем координату Y для центрирования окна: от высоты экрана отнимаем высоту окна и делим пополам, чтобы получить отступ сверху
# root.geometry(f"{window_width}x{window_height}+{x}+{y}")  # Устанавливаем размер и положение окна в формате "ширина x высота+X+Y", где x - это разделитель между шириной и высотой
#
# label_text = "Введите три числа и нажмите на кнопку для вычисления суммы"  # Текст для надписи
# Label(text=label_text, font='Arial 10').pack(pady=(15, 15))  # Создаём текстовую надпись с шрифтом Arial 10 и размещаем её с отступами сверху и снизу по 15 пикселей
#
# e1 = Entry(width=19, font='Arial 10') # Создаём первое поле для ввода шириной 19 символов шрифтом Arial 10
# e1.pack()  # размещаем первое поле ввода в окне программы
# e2 = Entry(width=19, font='Arial 10')  # Создаём второе поле для ввода шириной 19 символов шрифтом Arial 10
# e2.pack()  # размещаем второе поле ввода в окне программы
# e3 = Entry(width=19, font='Arial 10')  # Создаём третье поле для ввода шириной 19 символов шрифтом Arial 10
# e3.pack()  # размещаем третье поле ввода в окне программы
#
# def f1():  # Объявляем функцию с именем f1 (будет выполняться при нажатии на кнопку сложения)
#     a,b,c = float(e1.get()),float(e2.get()),float(e3.get())  # Получаем текст из трёх полей ввода (е1,е2,е3), преобразуем в числа с плавающей запятой float и сохраняем сразу в три переменные a, b, c
#     result = a + b + c  # Складываем три числа и сохраняем результат в переменную result
#     print(f"Результат сложения трёх чисел: {round(result, 6)}")  # Выводим в консоль сообщение с результатом, округлённым до 6 знаков после запятой (где round функция округления, result - число для округления, 6 - количество знаков после запятой)
#
# def f2():  # Объявляем функцию с именем f2 (будет выполняться при нажатии на кнопку умножения)
#     a,b,c = float(e1.get()),float(e2.get()),float(e3.get())  # Получаем текст из трёх полей ввода (е1,е2,е3), преобразуем в числа с плавающей запятой float и сохраняем сразу в три переменные a, b, c
#     result = a * b * c  # Умножаем три числа и сохраняем результат в переменную result
#     print(f"Результат умножения трёх чисел: {round(result, 6)}")  # Выводим в консоль сообщение с результатом, округлённым до 6 знаков после запятой (где round функция округления, result - число для округления, 6 - количество знаков после запятой)
#
# frame = Frame(root) # Создаём невидимый прямоугольник-контейнер внутри окна, чтобы поместить в него кнопки и управлять ими как группой
# frame.pack(pady=0)  # Размещаем контейнер с кнопками в окне без вертикальных отступов (прижимаем к другим элементам) .pack() - размещает контейнер в окне, а pady=0 - вертикальные отступы равны 0 пикселей - контейнер будет вплотную к предыдущему элементу (полям ввода)
#
# Button(frame, text="Сложить три числа", command=f1, width=15, font='Arial 10').pack(pady=(0))  # Создаём кнопку "Сложить три числа" внутри контейнера, шириной 15 символов, при нажатии выполняется функция f1, размещаем без отступов (где pady=(0) означает отступ сверху = 0 пикселей, снизу = 0 пикселей
# Button(frame, text="Умножить три числа", command=f2, width=16, font='Arial 10').pack(pady=(0, 5))  # Создаём кнопку "Умножить три числа" внутри контейнера, шириной 16 символов, при нажатии выполняется функция f2, размещаем с отступом снизу 5 пикселей (где pady=(0, 5) означает - отступ сверху 0, снизу 5)
#
# root.mainloop()   # Запускаем главный цикл программы - окно откроется и будет ждать действий пользователя (нажатий кнопок, ввода текста). Окно остаётся открытым и отзывчивым - программа реагирует на все действия пользователя в реальном времени.

"""Вариант 2 с выводом результата и в терминале и в окне интерфейса программы"""

# from tkinter import *  # импортируем все функции и классы из библиотеки tkinter
# from tkinter import messagebox  # импортируем модуль для создания всплывающих окон сообщений
# root = Tk()  # Создаём главное окно программы
# root.title("Калькулятор")  # Устанавливаем заголовок окна - текст "Калькулятор" будет показан в верхней строке окна
#
# screen_width = root.winfo_screenwidth()  # Узнаём ширину экрана в пикселях и сохраняем в переменную screen_width
# screen_height = root.winfo_screenheight()  # Узнаём высоту экрана пользователя в пикселях и сохраняем в переменную screen_height
# window_width = 400  # Задаём ширину окна программы в пикселях - 400 точек по горизонтали
# window_height = 230  # Задаём высоту окна программы в пикселях - 230 точек по вертикали
# x = (screen_width - window_width) // 2  # Вычисляем координату X для центрирования окна: от ширины экрана отнимаем ширину окна и делим пополам, чтобы получить отступ слева
# y = (screen_height - window_height) // 2  # Вычисляем координату Y для центрирования окна: от высоты экрана отнимаем высоту окна и делим пополам, чтобы получить отступ сверху
# root.geometry(f"{window_width}x{window_height}+{x}+{y}")  # Устанавливаем размер и положение окна в формате "ширина x высота+X+Y", где x - это разделитель между шириной и высотой
#
# label_text = "Введите три числа и нажмите на кнопку для вычисления суммы"  # Текст для надписи
# Label(text=label_text, font='Arial 10').pack(pady=(15, 20))  # Создаём текстовую надпись с шрифтом Arial 10 и размещаем её с отступами сверху 15 пикаселей и снизу 20пикаселей
#
# e1 = Entry(width=19, font='Arial 10')  # Создаём первое поле для ввода шириной 19 символов шрифтом Arial 10
# e1.pack()  # Размещаем первое поле ввода в окне программы
# e2 = Entry(width=19, font='Arial 10')  # Создаём второе поле для ввода шириной 19 символов шрифтом Arial 10
# e2.pack()  # Размещаем второе поле ввода в окне программы
# e3 = Entry(width=19, font='Arial 10')  # Создаём третье поле для ввода шириной 19 символов шрифтом Arial 10
# e3.pack()  # Размещаем третье поле ввода в окне программы
#
# def f1():  # Объявляем функцию с именем f1 (будет выполняться при нажатии на кнопку сложения)
#     a,b,c = float(e1.get()),float(e2.get()),float(e3.get())  # Получаем текст из трёх полей ввода (е1,е2,е3), преобразуем в числа с плавающей запятой float и сохраняем сразу в три переменные a, b, c
#     result = a + b + c  # Складываем три числа и сохраняем результат в переменную result
#     messagebox.showinfo("Результат", f"Результат сложения трёх чисел: {round(result, 4)}")  # Показываем всплывающее окно с результатом сложения, округлённым до 4 знаков после запятой
#     print(f"Результат сложения трёх чисел: {round(result, 4)}")  # Выводим в консоль сообщение с результатом, округлённым до 4 знаков после запятой
#
# def f2():  # Объявляем функцию с именем f2 (будет выполняться при нажатии на кнопку умножения)
#     a,b,c = float(e1.get()),float(e2.get()),float(e3.get())  # Получаем текст из трёх полей ввода (е1,е2,е3), преобразуем в числа с плавающей запятой float и сохраняем сразу в три переменные a, b, c
#     result = a * b * c  # Умножаем три числа и сохраняем результат в переменную result
#     messagebox.showinfo("Результат", f"Результат умножения трёх чисел: {round(result, 6)}")  # Показываем всплывающее окно с результатом умножения, округлённым до 6 знаков после запятой
#     print(f"Результат умножения трёх чисел: {round(result, 6)}")  # Выводим в консоль сообщение с результатом, округлённым до 6 знаков после запятой
#
# frame = Frame(root)  # Создаём невидимый прямоугольник-контейнер внутри окна, чтобы поместить в него кнопки и управлять ими как группой
# frame.pack(pady=0)  # Размещаем контейнер с кнопками в окне без вертикальных отступов (прижимаем к другим элементам)
#
# Button(frame, text="Сложить три числа", command=f1, width=15, font='Arial 10').pack(pady=(0))  # Создаём кнопку "Сложить три числа" внутри контейнера, шириной 15 символов, при нажатии выполняется функция f1, размещаем без отступов
# Button(frame, text="Умножить три числа", command=f2, width=16, font='Arial 10').pack(pady=(0))  # Создаём кнопку "Умножить три числа" внутри контейнера, шириной 16 символов, при нажатии выполняется функция f2, размещаем без отступов
#
# root.mainloop()  # Запускаем главный цикл программы - окно откроется и будет ждать действий пользователя (нажатий кнопок, ввода текста). Окно остаётся открытым и отзывчивым - программа реагирует на все действия пользователя в реальном времени.

"""Вариант 3 с выводом ошибок при неверном вводе (например текст вместо цифр или пустые поля, или цифры с текстом одновременно"""

from tkinter import *  # Импортируем все функции из библиотеки tkinter
from tkinter import messagebox  # Добавляем импорт для всплывающих окон

root = Tk()  # Создаём главное окно программы
root.title("Калькулятор")  # Устанавливаем заголовок окна - текст "Калькулятор" будет показан в верхней строке окна интерфейса программы

screen_width = root.winfo_screenwidth()  # Узнаём ширину экрана в пикселях и сохраняем в переменную screen_width
screen_height = root.winfo_screenheight()  # Узнаём высоту экрана пользователя в пикселях и сохраняем в переменную screen_height
window_width = 400  # Задаём ширину окна программы в пикселях - 400 точек по горизонтали
window_height = 230  # Задаём высоту окна программы в пикселях - 230 точек по вертикали
x = (screen_width - window_width) // 2  # Вычисляем координату X для центрирования окна: от ширины экрана отнимаем ширину окна и делим пополам, чтобы получить отступ слева
y = (screen_height - window_height) // 2  # Вычисляем координату Y для центрирования окна: от высоты экрана отнимаем высоту окна и делим пополам, чтобы получить отступ сверху
root.geometry(f"{window_width}x{window_height}+{x}+{y}")  # Устанавливаем размер и положение окна в формате "ширина x высота+X+Y", где x - это разделитель между шириной и высотой

label_text = "Введите три числа и нажмите на кнопку для вычисления суммы"  # Текст для надписи
Label(text=label_text, font='Arial 10').pack(pady=(15, 15))  # Создаём текстовую надпись с шрифтом Arial 10 и размещаем её с отступами сверху и снизу по 15 пикселей

e1 = Entry(width=19, font='Arial 10')  # Создаём первое поле для ввода шириной 19 символов шрифтом Arial 10
e1.pack()  # размещаем первое поле ввода в окне программы
e2 = Entry(width=19, font='Arial 10')  # Создаём второе поле для ввода шириной 19 символов шрифтом Arial 10
e2.pack()  # размещаем второе поле ввода в окне программы
e3 = Entry(width=19, font='Arial 10')  # Создаём третье поле для ввода шириной 19 символов шрифтом Arial 10
e3.pack()  # размещаем третье поле ввода в окне программы


def f1():  # Объявляем функцию с именем f1 (будет выполняться при нажатии на кнопку сложения)
    try:  # Начинаем блок обработки исключений - пробуем выполнить код, который может вызвать ошибку
        if not e1.get() or not e2.get() or not e3.get():  # Проверяем, что все поля заполнены
            messagebox.showerror("Ошибка", "Все поля должны быть заполнены!")  # Сообщаем о пустых полях
            return  # Прерываем выполнение функции
        # Пробуем преобразовать введённые значения в числа
        a, b, c = float(e1.get()), float(e2.get()), float(e3.get())  # Получаем текст из трёх полей ввода (е1,е2,е3), преобразуем в числа с плавающей запятой float и сохраняем сразу в три переменные a, b, c
        result = a + b + c  # Складываем три числа и сохраняем результат в переменную result

        # Выводим результат и в отдельное окно программы, и в терминал
        messagebox.showinfo("Результат", f"Результат сложения трёх чисел: {round(result, 6)}")  # Показываем всплывающее окно с результатом сложения, округлённым до 6 знаков после запятой
        print(f"Результат сложения трёх чисел: {round(result, 6)}")  # Выводим в консоль сообщение с результатом, округлённым до 6 знаков после запятой (где round функция округления, result - число для округления, 6 - количество знаков после запятой)

    except ValueError:  # Ловим конкретную ошибку - ValueError (возникает при невозможности преобразовать строку в число)
        messagebox.showerror("Ошибка", "Введите корректные числа! Проверьте, что в полях нет букв или символов.")  # Сообщаем о некорректном вводе


def f2():  # Объявляем функцию с именем f2 (будет выполняться при нажатии на кнопку умножения)
    try:  # Начинаем блок обработки исключений - пробуем выполнить код, который может вызвать ошибку
        # Проверяем, что все поля заполнены
        if not e1.get() or not e2.get() or not e3.get():
            messagebox.showerror("Ошибка", "Все поля должны быть заполнены!")  # Сообщаем о пустых полях
            return  # Прерываем выполнение функции

        # Пробуем преобразовать введённые значения в числа
        a, b, c = float(e1.get()), float(e2.get()), float(e3.get())  # Получаем текст из трёх полей ввода (е1,е2,е3), преобразуем в числа с плавающей запятой float и сохраняем сразу в три переменные a, b, c
        result = a * b * c  # Умножаем три числа и сохраняем результат в переменную result

        # Выводим результат и в отдельное окно программы, и в терминал
        messagebox.showinfo("Результат", f"Результат умножения трёх чисел: {round(result, 6)}")  # Показываем всплывающее окно с результатом умножения, округлённым до 6 знаков после запятой
        print(f"Результат умножения трёх чисел: {round(result, 6)}")  # Выводим в консоль сообщение с результатом, округлённым до 6 знаков после запятой (где round функция округления, result - число для округления, 6 - количество знаков после запятой)

    except ValueError:  # Ловим конкретную ошибку - ValueError (возникает при невозможности преобразовать строку в число)
        messagebox.showerror("Ошибка", "Введите корректные числа! Проверьте, что в полях нет букв или символов.")  # Сообщаем о некорректном вводе


frame = Frame(root)  # Создаём невидимый прямоугольник-контейнер внутри окна, чтобы поместить в него кнопки и управлять ими как группой
frame.pack(pady=0)  # Размещаем контейнер с кнопками в окне без вертикальных отступов (прижимаем к другим элементам) .pack() - размещает контейнер в окне, а pady=0 - вертикальные отступы равны 0 пикселей - контейнер будет вплотную к предыдущему элементу (полям ввода)

Button(frame, text="Сложить три числа", command=f1, width=15, font='Arial 10').pack(pady=(0))  # Создаём кнопку "Сложить три числа" внутри контейнера, шириной 15 символов, при нажатии выполняется функция f1, размещаем без отступов (где pady=(0) означает отступ сверху = 0 пикселей, снизу = 0 пикселей
Button(frame, text="Умножить три числа", command=f2, width=16, font='Arial 10').pack(pady=(0, 5))  # Создаём кнопку "Умножить три числа" внутри контейнера, шириной 16 символов, при нажатии выполняется функция f2, размещаем с отступом снизу 5 пикселей (где pady=(0, 5) означает - отступ сверху 0, снизу 5)

root.mainloop()  # Запускаем главный цикл программы - окно откроется и будет ждать действий пользователя (нажатий кнопок, ввода текста). Окно остаётся открытым и отзывчивым - программа реагирует на все действия пользователя в реальном времени.